# Docker Compose configuration for CARLA server
# TODO: Implement in Phase 1

version: '3.8'

services:
  carla-server:
    image: carlasim/carla:0.9.15
    ports:
      - "2000:2000"  # RPC port
      - "2001:2001"  # Streaming port
    # Additional configuration will be added in Phase 1
